type Mutation {
  article: ArticleMutations
}

type ArticleMutations {
  create(
    title: String!
    text: String!
  ): Article
  like(
    articleId: ID!
  ): Article
  removeLike(
    articleId: ID!
  ): Article
}
